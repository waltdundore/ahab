# Ahab (v2)

Reusable Ansible host-automation + build platform: `make install` provisions a box with a monolithic, self-integrated service stack; `make import <module>` imports content into it.

Start here: `CONTEXT.md` (state of record) → `SPEC.md` (platform contract + milestones M0–M6). `todo.md` is the append-only plan.

v1 lives on the `prod` branch — history and lessons only; see `CONTEXT.md` §3.
